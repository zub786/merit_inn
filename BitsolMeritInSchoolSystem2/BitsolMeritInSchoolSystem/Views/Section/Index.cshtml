@model IEnumerable<BitsolMeritInSchoolSystem.Models.Section>

@{
    ViewBag.Title = "Index";
    if (Session["Email"] == null)
    {

        Layout = "~/Views/Shared/_Layout.cshtml";
    }
    else
    {

        Layout = "~/Views/Shared/_Layout2.cshtml";
    }
}

<h2>Sections</h2>

<p>
    @Html.ActionLink("Add Section +", "Create", null, new { @class = "btn btn-primary" })
    
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SectionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SectionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.SectionId }, new { @class = "btn btn-sm btn-success" }) |
            @Html.ActionLink("Details", "Details", new { id = item.SectionId }, new { @class = "btn btn-sm btn-info" }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.SectionId }, new { @class = "btn btn-sm btn-danger" })
        </td>
    </tr>
}

</table>
